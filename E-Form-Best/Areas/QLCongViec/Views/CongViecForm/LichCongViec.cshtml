@{
    ViewData["Title"] = "Lịch Công Việc";
    Layout = "~/Areas/QLCongViec/Views/Shared/_Layout.cshtml";
}

<link href='https://cdn.jsdelivr.net/npm/fullcalendar@6.1.8/index.global.min.css' rel='stylesheet' />
<script src='https://cdn.jsdelivr.net/npm/fullcalendar@6.1.8/index.global.min.js'></script>

<style>
    .fc-event {
    cursor: pointer;
    border-radius: 6px;
    padding: 2px 5px;
    }

    .calendar-wrapper {
    background: white;
    padding: 30px;
    border-radius: 25px;
    box-shadow: 0 10px 25px -5px rgba(0,0,0,0.1);
    border: 1px solid #f1f5f9;
    }
    /* Chỉnh màu cho các nút bấm lịch sang màu xanh lá chủ đạo */
    .fc .fc-button-primary {
    background-color: #65a30d !important;
    border-color: #65a30d !important;
    }

    .fc .fc-button-primary:hover {
    background-color: #4d7c0f !important;
    }
</style>

<div style="max-width: 1200px; margin: 40px auto; padding: 0 20px;">
    <h2 style="font-weight: 900; color: #2d4201; margin-bottom: 25px;">
        <i class="fa fa-calendar" style="color: #65a30d;"></i> LỊCH THỰC HIỆN CÔNG VIỆC
    </h2>
    <div class="calendar-wrapper">
        <div id='calendar'></div>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function() {
    var calendarEl = document.getElementById('calendar');
    var calendar = new FullCalendar.Calendar(calendarEl, {
    initialView: 'dayGridMonth',
    locale: 'vi',
    headerToolbar: {
    left: 'prev,next today',
    center: 'title',
    right: 'dayGridMonth,timeGridWeek'
    },
    // Gọi đến API bạn đã viết trong region
    events: '/FormCongViec/GetLichCongViecData',
    eventClick: function(info) {
    // Ngăn chặn mở link mặc định nếu muốn xử lý khác
    if (info.event.url) {
    window.location.href = info.event.url;
    return false;
    }
    },
    eventContent: function(arg) {
    return { html: `<div style="font-size:12px; font-weight:700; white-space:nowrap; overflow:hidden;">${arg.event.title}</div>` };
    }
    });
    calendar.render();
    });
</script>